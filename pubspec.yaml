name: clinica_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: 10.6.0
  http: ^1.2.0
  image_picker: ^1.1.1
  cupertino_icons: ^1.0.8
  intl: ^0.18.1
  google_sign_in: ^6.1.5
  shared_preferences: ^2.2.2
  pdf: 3.11.3
  printing: ^5.9.0
  path_provider: ^2.0.15
  file_picker: ^5.2.5
  flutter_svg: ^2.0.7
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.2
  flutter_signin_button: ^2.0.0
  flutter_html: 3.0.0-beta.2
  uuid: ^3.0.6

  # Offline / sync helpers
  flutter_secure_storage: ^8.0.0
  connectivity_plus: ^4.0.1
  dio: ^5.1.0
  sembast: ^4.0.0
  drift: ^2.30.0
  drift_flutter: ^0.2.8
  sqlite3_flutter_libs: ^0.5.41
    # drift and related deps removed here to keep pubspec resolvable in CI.
    # drift and related deps removed here to keep pubspec resolvable in CI.

  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.4
  # build_runner and drift_dev removed; run codegen locally if needed.

flutter:
  uses-material-design: true

  # ------------------------------
  # Aquí agregamos la sección de assets
  # ------------------------------
  assets:
    - assets/images/logo.png
    - assets/images/           # cualquier otra imagen que agregues en esta carpeta

# Configuración para generar los iconos de la aplicación (launcher icons)
flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"

  # Puedes agregar también fuentes personalizadas si lo deseas
  # fonts:
  #   - family: MiFuente
  #     fonts:
  #       - asset: fonts/MiFuente-Regular.ttf
