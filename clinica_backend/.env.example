# Sample environment variables for clinica_backend
# Copy this to .env and fill with real values (DO NOT commit secrets)
# MySQL / Database (Railway plugin will provide these values in production)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=your_password_here
DB_NAME=clinica_db
PORT=3000

# Cloudinary (one of the two approaches below)
# Full connection URL (recommended):
# CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>
# Or set individual values:
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=

# Other envs the app may use
FRONTEND_URL=http://localhost:3001
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
# EMAIL_FROM=
